@model IEnumerable<CarTiresService.Models.Adresy>

@{
    ViewBag.Title = "Adresy";
}

<h2>Adresy</h2>

<p>
    @Html.ActionLink("Dodaj nowy adres", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Miasto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ulica)
        </th>
        <th>
            @Html.DisplayName("Numer domu")
        </th>
        <th>
            @Html.DisplayName("Numer mieszkania")
        </th>
        <th>
            @Html.DisplayName("Kod pocztowy")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Miasto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ulica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumerDomu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumerMieszkania)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KodPocztowy)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.AdresId }) |
            @Html.ActionLink("Szczegóły", "Details", new { id=item.AdresId }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.AdresId })
        </td>
    </tr>
}

</table>
